# Browser-facing API base. Leave unset to call /api (same origin).
NEXT_PUBLIC_API_URL=

# Server-only: proxy /api/* to FastAPI so the browser can keep using /api.
# Example: http://127.0.0.1:8000  or  https://api.events.ai
API_URL=
